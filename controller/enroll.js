const express = require('express')
const Enroll = require('../models/Enroll')

exports.createEnroll = async (req,res) =>{
    try{
      
    }
    catch(err){
        console.log(err);
        res.send("error");
    }
}